{
  "private": true,
  "scripts": {
    "build": "lerna run build --stream",
    "clean": "lerna run clean --stream"
  },
  "devDependencies": {
    "lerna": "^3.10.7",
    "typescript": "3.7.2"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
